import pkg.*;
import java.util.Scanner;
import java.util.Random;


class starter {
	public void generateWord(){
		String bob = generateWord;
		int tries = 5;
	}
	public void integerconstructor(int numtries){
		Scanner sc = new Scanner(System.in);
		System.out.println("How many tries do you want to have?");
		numtries = sc.nextInt;
		if(numtries = 0){
			numtries = tries;
		}
		else{
			tries = numtries;
		}
	}
	public int guessCheck(String letter, int ){
		
	}
}
